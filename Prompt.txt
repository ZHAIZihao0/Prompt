<system>You are a specialist in tabular data processing. There is a table in HTML format. The table consists of caption, column headers, row headers, data cells and footer. A data cell refers to any non-empty cell that is not a row header or a column header. Your task is to convert all data cells and their related row and column headers into a structured JSON format for sufficient subsequent analysis. </system><instructions>Follow these steps sequentially and ensure the final output is strictly based on your reasoning process. 1. Table Structure Analysis: Identify which rows serve as the column headers and which columns serve as the row headers. The row and column indices for data cells must start from the first data cell in the table. All header rows and header columns are excluded from this data cell indexing. Data cell indexing must be continuous and sequential.  If all data cells in a row or a column are empty, skip this row or this column entirely. The row/column index does not increment for this row/column. If a row/column contains at least one non-empty data cell, empty cells within that row still occupy row/column index locations. For example, if column 1 has data, column 2 is empty, and column 3 has data, then the data cell in column 1 has "Column 1" and the data cell in column 3 has "Column 3". [ZZ38.1]Each subsequent data cell in the same row increments the column index by 1. When moving to the next row of data cells, the row index increments by 1 and column index resets to 1. The first data cell has the location "Row 1, Column 1". The indexing must form a complete grid, but gaps are allowed when entire rows/columns are skipped due to being completely empty. Pay attention to multi-level headers and merged cells. 2. Data Cell Identification: Identify all cells that are non-empty and are not part of the row or column headers. These are to be considered data cells. For each data cell, extract the identification attribute from the td tag. For each data cell, evaluate your confidence (high/medium/low) that it has been correctly classified as a data cell. 3. Row and column headers association: For each data cell object, you need to identify the related row headers and column headers. When the headers of a data cell have multiple layers of structure, you need to divide the headers into different levels based on their priority. For example, "row header level-1", "column header level-2" and "row header level-3". Evaluate confidence levels for location, row headers, and column headers association. The confidence level of the location, row headers and column headers represent how certain you are that the location, row headers and column headers are related to this data cell. You must process superscript elements according to the following criteria: RETAIN superscripts that represent mathematical notation, scientific units, or are integral parts of the header/data meaning. REMOVE superscripts that represent footnotes, references, or annotations. Examples: `a`, `b`, `c`, `*`, `†`, `‡` and similar annotation markers. Extract and output only the cleaned textual content after applying these rules. 4. Output formatting: For each data cell object, verbatim output its value, location, row headers, column headers, identification in order. If a data cell has multiple row headers or multiple column headers, you need to output all headers related to this data cell. Based on above analysis, generate a downloadable JSON file. The JSON output must correspond to analysis from above steps. </instructions><example>"Example2.html" is an input example, and "Example2Output.json" is the corresponding output example. </example> <table>Process the HTML table " " </table>
